# Private Scripts Omitted

This directory is intentionally kept as a public placeholder.

The original private project contains internal maintenance workflows for data ingestion, normalization, synchronization, and embedding generation. Those implementations are not included in this open-source web shell.

You can replace these placeholder files with your own pipeline if you want to build on top of this repository.
